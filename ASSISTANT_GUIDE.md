# Hermes 助手界面使用说明

右侧默认紧凑排版，正文12px、侧栏364px。可拖动左侧分隔线调宽，可选85/90/100密度，也可展开阅读；这些是当前浏览器的显示偏好，不改变业务配置。

对话与工具活动分开，工具详情默认折叠。批准和补充信息在输入区上方的小卡里；查看长参数不会清空回答或隐藏输入。批准始终针对当前有效请求，完整详情弹窗仅供阅读。

回复支持Markdown表格、列表、引用和代码。宽表格局部横向滚动，代码可复制。模型HTML不执行，远端图片不自动加载；没有数学公式或Mermaid渲染。上翻阅读时新消息不抢位置，可点击“回到最新消息”。

关闭/刷新页面只改变观察，不重新发送任务。停止对应Hermes任务不停止数据与图表，也不改变自动任务开关。邮件和飞书仍由Hermes中的Skills执行。本包不包含生成系统或生成器管理员账号。
