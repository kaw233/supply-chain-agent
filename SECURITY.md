# 安全策略

## 支持范围

当前仅维护最新主分支。项目尚未承诺生产级安全支持或长期版本维护。

## 报告漏洞

请不要在公开 Issue 中披露可利用细节、凭据或企业数据。优先使用代码托管平台提供的私密漏洞报告功能；如果尚未配置，请通过仓库所有者指定的私密渠道联系维护者。报告请包含受影响版本、复现步骤、影响范围和建议缓解方式。维护者确认安全联系渠道后，应在公开发布页补充明确的联系人或 Security Advisory 入口。

## 部署边界

- `.env`、`data/`、`reports/`、SQLite 数据库和日志均为本地运行产物，不应提交或随源码发布。
- `examples/` 与 `packs/` 只能包含全合成数据。人员、邮箱、组织和业务编号应遵守 `docs/数据脱敏说明.md`，并通过自动脱敏审计。
- DSH 的 `workspace-write` 是工具策略，不是操作系统沙箱。生产部署必须使用专用系统账户、最小文件权限、独立密钥和受控网络出口。
- 工作台可调用外部 Agent、MCP 和 Skills。批准一次任务不代表永久授权，也不保证已经发生的外部动作可以撤回。
- 默认服务监听 `127.0.0.1`。若监听公网或内网共享地址，应在前置网关增加 TLS、认证、访问控制、速率限制和审计。
- 当前 RAG 为合同框架，未配置真实数据与 Provider；不要把 `not_configured` 状态解释为安全或质量验收通过。
- 示例值 `fixture-key`、`fixture-password` 仅用于本地协议测试，不能用于真实环境。

发现密钥误提交时，应先在提供方撤销并轮换密钥，再清理历史记录；仅删除当前文件不足以消除泄露。
